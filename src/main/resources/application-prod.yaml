spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:postgresql://${ADDRESS_DB_HOST}:${ADDRESS_DB_PORT}/${ADDRESS_DB_NAME}?sslmode=${ADDRESS_DB_SSL_MODE:require}&ApplicationName=address-system
    username: ${ADDRESS_DB_USER}
    password: ${ADDRESS_DB_PASSWORD}
    hikari:
      pool-name: address-system-pool
      maximum-pool-size: ${DB_POOL_MAX_SIZE:10}
      minimum-idle: ${DB_POOL_MIN_IDLE:2}
      connection-timeout: ${DB_CONNECTION_TIMEOUT:5000}
      connection-init-sql: SET search_path TO address, public

management:
  endpoint:
    health:
      show-details: never

logging:
  level:
    com.address.address_system: ${APP_LOG_LEVEL:INFO}
